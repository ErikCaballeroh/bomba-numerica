import { ModuleScaffold } from '../common/ModuleScaffold'

export const NoLinealesSecanteModule = (props) => (
  <ModuleScaffold
    {...props}
    topic="Ecuaciones no lineales"
    title="Secante"
    description="Lienzo para el metodo de la secante."
  />
)
